{
  "name": "ravenandcedar-site",
  "version": "1.0.0",
  "scripts": {
    "build": "node generate-manifest.js && eleventy",
    "start": "node generate-manifest.js && eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "luxon": "^3.0.0"
  }
}
